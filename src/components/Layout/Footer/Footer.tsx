import * as React from 'react';

const Footer: React.FC = () => {
  return (
    <footer className="mt-12 text-secondaryText text-xs mb-3 text-center">
      <small>Weather app</small>
    </footer>
  );
};

export default Footer;
